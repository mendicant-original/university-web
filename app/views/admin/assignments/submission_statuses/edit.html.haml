%h1.form-header Edit Assignment Submission Status

= form_for @submission_status, :html => {:class => "bp"},
  :url => admin_assignments_submission_status_path(@submission_status) do |f|
  = render :partial => "form", :locals => {:f => f}