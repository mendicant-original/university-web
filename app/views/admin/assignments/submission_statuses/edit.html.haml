%h1 Edit Assignment Submission Status

= form_for @submission_status, 
  :url => admin_assignments_submission_status_path(@submission_status) do |f|
  = render :partial => "form", :locals => {:f => f}