%p
  = f.label :name
  = f.text_field :name
  
%p
  = f.label :sort_order
  = f.text_field :sort_order
  
%p.submit
  = f.submit @submission_status.new_record? ? 'Create' : 'Save'
  = link_to 'Cancel', admin_assignments_submission_statuses_path, :class => "cancel"
