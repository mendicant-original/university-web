%h1 Courses

= button_to "Create Course", new_admin_course_path, :method => :get

%table.edit
  %thead
    %tr
      %th Name
      %th 
  %tbody
    - @courses.each do |course|
      %tr{:class => cycle('even','odd')}
        %td= link_to course.name, edit_admin_course_path(course)
        %td= link_to("Destroy", admin_course_path(course), :confirm => "Are you sure you wish to delete this course?", :method => 'DELETE')