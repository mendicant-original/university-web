%h1 Directory

#directory
  - @users.each do |user|
    .user
      .icon= image_tag user.gravatar_url(48)
      .name= link_to user.name, user_path(user)
      .since= user_since(user)
      
      .clear

= will_paginate @users