!!! Strict
%html
  %head
    %title
      = yield(:title).blank? ? controller_name.humanize : yield(:title)
      \- RMU
    = stylesheet_tag 'screen', :media => :screen
    = javascript_include_tag 'jquery-1.4.2.js', 'jquery.scrollTo', 'application', 'rails'
    = csrf_meta_tag
    
    = yield(:header_bottom)

  %body
    #container
      = render :partial => "/admin/menu"
      #content
        #header= yield(:header)
      
        = yield